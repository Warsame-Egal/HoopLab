# Leave empty for same-origin (recommended): Vite/nginx proxy /api and /ws to Spring on :8080
VITE_API_BASE_URL=
VITE_WS_URL=
# Or point directly at Spring when not using the dev proxy:
# VITE_API_BASE_URL=http://localhost:8080
# VITE_WS_URL=ws://localhost:8080
